---
title: Export Overview
description: Export Motionly projects from the browser editor.
---

# Export Overview

Motionly exposes MP4 export from the editor toolbar.

Current export behavior:

- Uses the project canvas resolution
- Uses the project FPS
- Shows export progress
- Downloads a file named after the current project

The current browser export path runs in real time. Keep the editor tab active while exporting.

